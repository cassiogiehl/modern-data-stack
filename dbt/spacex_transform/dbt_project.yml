name: 'spacex_transform'

version: '1.0.0'
config-version: 2

profile: 'spacex_transform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
  - logs

models:
  spacex_transform:
    launches:
      +materialized: table

# sources:
#   - name: launches
#     database: data_warehouse
#     schema: public
#     tables:
#       - name: launches
#       - name: rockets